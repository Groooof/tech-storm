services:
  techstorm-frontend-dev:
    container_name: techstorm-frontend-dev
    restart: always
    image: techstorm-frontend-dev
    ports:
      - 80:80
    depends_on:
      - techstorm-backend

  techstorm-backend:
    container_name: techstorm-backend
    restart: always
    image: techstorm-backend
    ports:
      - 8001:8001
    env_file:
      - .env.dev
    depends_on:
      - techstorm-db-dev

  techstorm-db-dev:
    container_name: techstorm-db-dev
    restart: always
    image: postgres:16
    expose:
      - 5432
    ports:
      - 5555:5432
    env_file:
      - .env.dev
    volumes:
      - /opt/techstorm/postgresql/data:/var/lib/postgresql/data

  techstorm-migrate:
    container_name: techstorm-migrate
    command: alembic upgrade head
    image: techstorm-backend
    env_file:
      - .env.dev
    depends_on:
      - techstorm-db-dev